<p id="notice"><%= notice %></p>
<h2>sessions</h2>

The current user is: <%= p @current_user.first_name %>


<% @sessions.each do |session| %>


  <p>
    <strong>Title:</strong>
    <%= session.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= session.description %>
  </p>


  <p>
    <strong>Favorite?:</strong>
    <%= session.is_favorite %>
  </p>

  <%= link_to 'Edit', edit_session_path(session) %>

<% end %>
